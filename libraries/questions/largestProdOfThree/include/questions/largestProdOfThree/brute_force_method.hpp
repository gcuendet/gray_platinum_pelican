#include <vector>

/**
 * Computes the largest product of three int from a list by computing all products
 *
 * Time complexity is O(N^3) and
 * Space complexity is O(1)
 */
int largest_product_brute_force(const std::vector<int>& input);
