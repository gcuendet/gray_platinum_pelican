#include <vector>

/**
 * Computes the largest product of three int from a list by sorting the list
 *
 * Time complexity is O(N logN) and
 * Space complexity is O(N)
 *
 * Algo:
 * -----
 * - sort
 */
int largest_product_sort(const std::vector<int>& input);
